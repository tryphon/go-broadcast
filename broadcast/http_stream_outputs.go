package broadcast

import ()

type HttpStreamOutputs struct {
	outputs []HttpStreamOutput
}

// func (splitter *Splitter) AudioOut(audio *Audio) {
// 	for _, handler := range splitter.handlers {
// 		handler.AudioOut(audio)
// 	}
// }
